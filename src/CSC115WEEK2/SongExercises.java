package CSC115WEEK2;

public class SongExercises {

	/*
	 * Purpose: determine whether the array contains a song by given artist
	 * Parameters: Song[] array - the array to search through
	 *             String artistName - the artist name to search for
	 * Returns: boolean - true if there is a song by the given artist, false otherwise
	 */
	public static boolean containsArtist(Song[] array, String artistName) {
		boolean found = false;
				
		return found; // so it compiles
	}
	
	/*
	 * Purpose: get the sum of all song durations found in the array
	 * Parameters: Song[] songs - the array the search through
	 * Returns: int - the sum of all song durations
	 */
	public static int getPlaylistLength(Song[] array) {
		int result = 0;
		
		return result; // so it compiles
	}

}